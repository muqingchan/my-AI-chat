# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交第一次版本
git commit -m "initial commit"

# 设置远程仓库地址（替换成你自己的 GitHub 地址！）
git remote add origin https://github.com/你的用户名/my-ai-chat.git

# 推送到 GitHub 主分支
git branch -M main
git push -u origin main# my-AI-chat
